<h1>Burgers</h1>

<h2>burgers Ordered</h2>

<ul>
{{#each burgers}}
{{#unless devoured}}
{{burgers devoured=true}}
{{/unless}}
{{/each}}
</ul>

<h2>Burgers devaoured</h2>
<ul>
{{#each burgers}}
{{#unless devoured}}
{{burgers devoured=false}}
{{/unless}}
{{/each}}
</ul>

<form>
<div class="form-group">
    <label for="burg">Example textarea</label>
    <textarea class="form-control" id="burg" rows="3"></textarea>
  </div>
  <button type="submit">Add Burger</button>

<div class="form-group">
    <label for="devoy">Example textarea</label>
    <textarea class="form-control" id="denvoy" rows="3"></textarea>
  </div>
  <button type="submit">EAT</button>

</form>